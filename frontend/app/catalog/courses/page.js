import CourseManagement from '@/features/course/CourseManagement';

export const metadata = { title: 'All Courses — Xebia LMS' };

export default function AllCoursesPage() {
  return <CourseManagement />;
}
