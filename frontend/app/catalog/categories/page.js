import CategoryManagement from '@/features/category/CategoryManagement';

export const metadata = { title: 'Categories — Xebia LMS' };

export default function CategoriesPage() {
  return <CategoryManagement />;
}
